

/*
click button to play
prompt for word
determine if any non letters in array
if yes => request new word (letters only)
if no => store word as array

input field to guess letter
click event triggers:
- take in value
- determine if it is a letter (not a number or other char)
- if no => request different character
- if yes
  - loop through array and find the first match
  - if match => display "Nice!"
  - if no match => decrement counter, hide "Nice!"


*/
